//TODO serialize json into camelcase from snake case.
export interface TickerData {
    max_price: number,
    avg_price: number,
    min_price: number,
    max_volume: number,
    avg_volume: number,
    min_volume: number,
}